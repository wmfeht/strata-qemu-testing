# strata-qemu-testing

Scripts and configurations for testing a GTK desktop application and its installer across Linux distributions.

This repository holds the QEMU/guest setup used to run those tests: VM definitions, provisioning, installer runs, and desktop-session checks against multiple distros.
